You're building a vehicle rental system. There are three vehicle types: Car, Bike, and Truck.
All implement a Vehicle interface with a rent() method.
Write a VehicleFactory with a static method getVehicle(String type).
What should happen if someone requests a "helicopter"?