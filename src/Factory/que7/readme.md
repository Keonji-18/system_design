You're building a UI toolkit that must support two themes: LightTheme and DarkTheme. Each theme has a Button and a Checkbox.
Define the UIFactory interface, both concrete factories, and the product interfaces. Show how a Application class uses the factory without knowing which theme is active.
